name: CI

on:
 
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  
  build:
    
    runs-on: ubuntu-latest

    steps:
      
      - uses: actions/checkout@v3
      
      - name: Check Contract Polices.
        run: |
<<<<<<< HEAD
          chmod +x ./contract-polices.sh
          ./contract-polices.sh
=======
          chmod +x ./check-contract.sh
          ./check-contract.sh
>>>>>>> refs/remotes/origin/master
        shell: bash
 
        
         
         
          
         
